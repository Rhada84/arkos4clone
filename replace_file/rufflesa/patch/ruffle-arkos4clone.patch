diff --git a/sdl2test-u/src/main.rs b/sdl2test-u/src/main.rs
index 67033d6..952f59b 100755
--- a/sdl2test-u/src/main.rs
+++ b/sdl2test-u/src/main.rs
@@ -9,6 +9,7 @@ use std::{
     thread::sleep,
     time::Duration,
     time::Instant,
+    process::Command,
 };
 
 use sdl2::controller::{Button, Axis};
@@ -142,16 +143,9 @@ fn read_file_to_vec<P: AsRef<Path>>(path: P) -> Result<Vec<u8>, Error> {
 #[derive(Parser, Debug)]
 #[clap(name = "Offscreen", author, version)]
 struct Opt {
-    // The swf file
     #[clap(name = "swf")]
     swf: PathBuf,
 
-    #[clap(short = 'W', long = "width", default_value = "640")]
-    width: u32,
-
-    #[clap(short = 'H', long = "height", default_value = "480")]
-    height: u32,
-
     #[clap(short = 'Q', long = "quality", default_value = "default")]
     quality: String,
 
@@ -159,6 +153,20 @@ struct Opt {
     no_audio: bool,
 }
 
+fn detect_resolution() -> Option<(u32, u32)> {
+    let output = Command::new("/usr/local/bin/console_detect").arg("-r").output().ok()?;
+    let res = String::from_utf8_lossy(&output.stdout).trim().to_string();
+    let parts: Vec<&str> = res.split('x').collect();
+    if parts.len() == 2 {
+        if let (Ok(w), Ok(h)) = (parts[0].parse::<u32>(), parts[1].parse::<u32>()) {
+            if w > 0 && h > 0 {
+                return Some((w, h));
+            }
+        }
+    }
+    None
+}
+
 //const TARGET_FPS: u64 = 60; // 目标帧率
 const FRAME_TIME: Duration = Duration::from_millis(16);
 const TRIGGER_PRESS_THRESHOLD:i16 = 0x7000;  // 按下阈值
@@ -205,7 +213,16 @@ async fn main() -> Result<(), String> {
     let video_subsystem = sdl_context.video().unwrap();
     let game_controller_subsystem = sdl_context.game_controller().unwrap();
 
-    let (width, height) = (opt.width as u32, opt.height as u32);
+    let (width, height) = match detect_resolution() {
+        Some((w, h)) => {
+            println!("Detected resolution: {}x{}", w, h);
+            (w, h)
+        }
+        None => {
+            eprintln!("console_detect not found or failed, exiting.");
+            std::process::exit(0);
+        }
+    };
     let quality : StageQuality = match opt.quality.as_str() {
         "low" => StageQuality::Low,
         "medium" => StageQuality::Medium,
@@ -213,8 +230,8 @@ async fn main() -> Result<(), String> {
     };
 
     let gl_attr = video_subsystem.gl_attr();
-    gl_attr.set_context_profile(GLProfile::Core);
-    gl_attr.set_context_version(4, 1);
+    gl_attr.set_context_profile(GLProfile::GLES);
+    gl_attr.set_context_version(3, 2);
 
     let window = video_subsystem
         .window("Ruffle SDL2 Player", width, height)
@@ -671,9 +688,10 @@ async fn main() -> Result<(), String> {
                     }
 
                     match button {
-                        Button::Guide => {
-                            println!("Guide 键按下退出");
-                            break 'running;
+                        Button::Start => {
+                            if backbtn_pressed {
+                                break 'running;  // Select+Start 退出
+                            }
                         }
                         Button::X => {
                             if backbtn_pressed {
@@ -681,7 +699,7 @@ async fn main() -> Result<(), String> {
                                 backbtn_pressed = false;
                             }
                         }
-                        Button::Start => {
+                        Button::A => {
                             if backbtn_pressed {
                                 dpad2wasd = !dpad2wasd;
                                 backbtn_pressed = false;
@@ -825,6 +843,54 @@ async fn main() -> Result<(), String> {
                                 }
                             }
                         },
+                        Axis::LeftX => {
+                            if let Some(player) = playerbox.as_ref() {
+                                if value < -16384 {
+                                    let kc = if dpad2wasd { Keycode::A } else { Keycode::Left };
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyDown { key: kd });
+                                    }
+                                } else if value > 16384 {
+                                    let kc = if dpad2wasd { Keycode::D } else { Keycode::Right };
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyDown { key: kd });
+                                    }
+                                } else {
+                                    let kc_l = if dpad2wasd { Keycode::A } else { Keycode::Left };
+                                    let kc_r = if dpad2wasd { Keycode::D } else { Keycode::Right };
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc_l) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyUp { key: kd });
+                                    }
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc_r) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyUp { key: kd });
+                                    }
+                                }
+                            }
+                        },
+                        Axis::LeftY => {
+                            if let Some(player) = playerbox.as_ref() {
+                                if value < -16384 {
+                                    let kc = if dpad2wasd { Keycode::W } else { Keycode::Up };
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyDown { key: kd });
+                                    }
+                                } else if value > 16384 {
+                                    let kc = if dpad2wasd { Keycode::S } else { Keycode::Down };
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyDown { key: kd });
+                                    }
+                                } else {
+                                    let kc_u = if dpad2wasd { Keycode::W } else { Keycode::Up };
+                                    let kc_d = if dpad2wasd { Keycode::S } else { Keycode::Down };
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc_u) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyUp { key: kd });
+                                    }
+                                    if let Some(kd) = keycodes::sdl2_keycode_to_ruffle_key_descriptor(kc_d) {
+                                        player.player.lock().unwrap().handle_event(PlayerEvent::KeyUp { key: kd });
+                                    }
+                                }
+                            }
+                        },
                         _ => {}
                     }
 
