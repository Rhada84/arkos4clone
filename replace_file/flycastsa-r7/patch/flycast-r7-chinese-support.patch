diff --git a/core/rend/gui.cpp b/core/rend/gui.cpp
index 94840d21e..a1dca9880 100644
--- a/core/rend/gui.cpp
+++ b/core/rend/gui.cpp
@@ -19,6 +19,7 @@
 
 #include <mutex>
 #include "gui.h"
+#include "i18n.h"
 #include "osd.h"
 #include "cfg/cfg.h"
 #include "hw/maple/maple_if.h"
@@ -171,7 +172,7 @@ void gui_init()
         0,
     };
 
-    io.Fonts->AddFontFromMemoryCompressedTTF(roboto_medium_compressed_data, roboto_medium_compressed_size, 17.f * scaling, nullptr, ranges);
+    io.Fonts->AddFontFromMemoryCompressedTTF(roboto_medium_compressed_data, roboto_medium_compressed_size, 20.f * scaling, nullptr, ranges);
     ImFontConfig font_cfg;
     font_cfg.MergeMode = true;
 #ifdef _WIN32
@@ -182,33 +183,33 @@ void gui_init()
     case 932:	// Japanese
 		{
 			font_cfg.FontNo = 2;	// UIGothic
-			ImFont* font = io.Fonts->AddFontFromFileTTF((fontDir + "msgothic.ttc").c_str(), 17.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesJapanese());
+			ImFont* font = io.Fonts->AddFontFromFileTTF((fontDir + "msgothic.ttc").c_str(), 20.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesJapanese());
 			font_cfg.FontNo = 2;	// Meiryo UI
 			if (font == nullptr)
-				io.Fonts->AddFontFromFileTTF((fontDir + "Meiryo.ttc").c_str(), 17.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesJapanese());
+				io.Fonts->AddFontFromFileTTF((fontDir + "Meiryo.ttc").c_str(), 20.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesJapanese());
 		}
 		break;
     case 949:	// Korean
 		{
-			ImFont* font = io.Fonts->AddFontFromFileTTF((fontDir + "Malgun.ttf").c_str(), 17.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesKorean());
+			ImFont* font = io.Fonts->AddFontFromFileTTF((fontDir + "Malgun.ttf").c_str(), 20.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesKorean());
 			if (font == nullptr)
 			{
 				font_cfg.FontNo = 2;	// Dotum
-				io.Fonts->AddFontFromFileTTF((fontDir + "Gulim.ttc").c_str(), 17.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesKorean());
+				io.Fonts->AddFontFromFileTTF((fontDir + "Gulim.ttc").c_str(), 20.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesKorean());
 			}
 		}
     	break;
     case 950:	// Traditional Chinese
 		{
 			font_cfg.FontNo = 1; // Microsoft JhengHei UI Regular
-			ImFont* font = io.Fonts->AddFontFromFileTTF((fontDir + "Msjh.ttc").c_str(), 17.f * scaling, &font_cfg, GetGlyphRangesChineseTraditionalOfficial());
+			ImFont* font = io.Fonts->AddFontFromFileTTF((fontDir + "Msjh.ttc").c_str(), 20.f * scaling, &font_cfg, GetGlyphRangesChineseTraditionalOfficial());
 			font_cfg.FontNo = 0;
 			if (font == nullptr)
-				io.Fonts->AddFontFromFileTTF((fontDir + "MSJH.ttf").c_str(), 17.f * scaling, &font_cfg, GetGlyphRangesChineseTraditionalOfficial());
+				io.Fonts->AddFontFromFileTTF((fontDir + "MSJH.ttf").c_str(), 20.f * scaling, &font_cfg, GetGlyphRangesChineseTraditionalOfficial());
 		}
     	break;
     case 936:	// Simplified Chinese
-		io.Fonts->AddFontFromFileTTF((fontDir + "Simsun.ttc").c_str(), 17.f * scaling, &font_cfg, GetGlyphRangesChineseSimplifiedOfficial());
+		io.Fonts->AddFontFromFileTTF((fontDir + "Simsun.ttc").c_str(), 20.f * scaling, &font_cfg, GetGlyphRangesChineseSimplifiedOfficial());
     	break;
     default:
     	break;
@@ -221,19 +222,19 @@ void gui_init()
     
     if (locale.find("ja") == 0)             // Japanese
     {
-        io.Fonts->AddFontFromFileTTF((fontDir + "ヒラギノ角ゴシック W4.ttc").c_str(), 17.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesJapanese());
+        io.Fonts->AddFontFromFileTTF((fontDir + "ヒラギノ角ゴシック W4.ttc").c_str(), 20.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesJapanese());
     }
     else if (locale.find("ko") == 0)       // Korean
     {
-        io.Fonts->AddFontFromFileTTF((fontDir + "AppleSDGothicNeo.ttc").c_str(), 17.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesKorean());
+        io.Fonts->AddFontFromFileTTF((fontDir + "AppleSDGothicNeo.ttc").c_str(), 20.f * scaling, &font_cfg, io.Fonts->GetGlyphRangesKorean());
     }
     else if (locale.find("zh-Hant") == 0)  // Traditional Chinese
     {
-        io.Fonts->AddFontFromFileTTF((fontDir + "PingFang.ttc").c_str(), 17.f * scaling, &font_cfg, GetGlyphRangesChineseTraditionalOfficial());
+        io.Fonts->AddFontFromFileTTF((fontDir + "PingFang.ttc").c_str(), 20.f * scaling, &font_cfg, GetGlyphRangesChineseTraditionalOfficial());
     }
     else if (locale.find("zh-Hans") == 0)  // Simplified Chinese
     {
-        io.Fonts->AddFontFromFileTTF((fontDir + "PingFang.ttc").c_str(), 17.f * scaling, &font_cfg, GetGlyphRangesChineseSimplifiedOfficial());
+        io.Fonts->AddFontFromFileTTF((fontDir + "PingFang.ttc").c_str(), 20.f * scaling, &font_cfg, GetGlyphRangesChineseSimplifiedOfficial());
     }
 #elif defined(__ANDROID__)
     if (getenv("FLYCAST_LOCALE") != nullptr)
@@ -251,10 +252,29 @@ void gui_init()
         	glyphRanges = GetGlyphRangesChineseSimplifiedOfficial();
 
         if (glyphRanges != nullptr)
-        	io.Fonts->AddFontFromFileTTF("/system/fonts/NotoSansCJK-Regular.ttc", 17.f * scaling, &font_cfg, glyphRanges);
+        	io.Fonts->AddFontFromFileTTF("/system/fonts/NotoSansCJK-Regular.ttc", 20.f * scaling, &font_cfg, glyphRanges);
     }
 
-    // TODO Linux...
+#elif defined(__linux__)
+    if (isChineseLocale())
+    {
+        const char* fontPaths[] = {
+            "/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc",
+            "/usr/share/fonts/noto-cjk/NotoSansCJK-Regular.ttc",
+            "/usr/share/fonts/google-noto-cjk/NotoSansCJK-Regular.ttc",
+            "/usr/share/fonts/truetype/noto/NotoSansCJK-Regular.ttc",
+            "/usr/share/fonts/truetype/droid/DroidSansFallback.ttf",
+            "/usr/share/fonts/wqy-zenhei/wqy-zenhei.ttc",
+            "/usr/share/fonts/wqy-microhei/wqy-microhei.ttc",
+            nullptr
+        };
+        for (int i = 0; fontPaths[i] != nullptr; i++)
+        {
+            ImFont* font = io.Fonts->AddFontFromFileTTF(fontPaths[i], 20.f * scaling, &font_cfg, GetGlyphRangesChineseSimplifiedOfficial());
+            if (font != nullptr)
+                break;
+        }
+    }
 #endif
     INFO_LOG(RENDERER, "Screen DPI is %d, size %d x %d. Scaling by %.2f", screen_dpi, screen_width, screen_height, scaling);
 
@@ -425,13 +445,13 @@ static void gui_display_commands()
         ImGui::PushItemFlag(ImGuiItemFlags_Disabled, true);
         ImGui::PushStyleVar(ImGuiStyleVar_Alpha, ImGui::GetStyle().Alpha * 0.5f);
 	}
-	if (ImGui::Button("Load State", ImVec2(150 * scaling, 50 * scaling)))
+	if (ImGui::Button(tr("Load State"), ImVec2(150 * scaling, 50 * scaling)))
 	{
 		gui_state = GuiState::Closed;
 		dc_loadstate();
 	}
 	ImGui::NextColumn();
-	if (ImGui::Button("Save State", ImVec2(150 * scaling, 50 * scaling)))
+	if (ImGui::Button(tr("Save State"), ImVec2(150 * scaling, 50 * scaling)))
 	{
 		gui_state = GuiState::Closed;
 		dc_savestate();
@@ -443,18 +463,18 @@ static void gui_display_commands()
 	}
 
 	ImGui::NextColumn();
-	if (ImGui::Button("Settings", ImVec2(150 * scaling, 50 * scaling)))
+	if (ImGui::Button(tr("Settings"), ImVec2(150 * scaling, 50 * scaling)))
 	{
 		gui_state = GuiState::Settings;
 	}
 	ImGui::NextColumn();
-	if (ImGui::Button("Resume", ImVec2(150 * scaling, 50 * scaling)))
+	if (ImGui::Button(tr("Resume"), ImVec2(150 * scaling, 50 * scaling)))
 	{
 		gui_state = GuiState::Closed;
 	}
 
 	ImGui::NextColumn();
-	const char *disk_label = libGDR_GetDiscType() == Open ? "Insert Disk" : "Eject Disk";
+	const char *disk_label = libGDR_GetDiscType() == Open ? tr("Insert Disk") : tr("Eject Disk");
 	if (ImGui::Button(disk_label, ImVec2(150 * scaling, 50 * scaling)))
 	{
 		if (libGDR_GetDiscType() == Open)
@@ -468,12 +488,12 @@ static void gui_display_commands()
 		}
 	}
 	ImGui::NextColumn();
-	if (ImGui::Button("Cheats", ImVec2(150 * scaling, 50 * scaling)))
+	if (ImGui::Button(tr("Cheats"), ImVec2(150 * scaling, 50 * scaling)))
 	{
 		gui_state = GuiState::Cheats;
 	}
 	ImGui::Columns(1, nullptr, false);
-	if (ImGui::Button("Exit", ImVec2(300 * scaling + ImGui::GetStyle().ColumnsMinSpacing + ImGui::GetStyle().FramePadding.x * 2 - 1,
+	if (ImGui::Button(tr("Exit"), ImVec2(300 * scaling + ImGui::GetStyle().ColumnsMinSpacing + ImGui::GetStyle().FramePadding.x * 2 - 1,
 			50 * scaling)))
 	{
 		if (!commandLineStart)
@@ -627,13 +647,13 @@ static void detect_input_popup(int index, bool analog)
 	ImVec2 padding = ImVec2(20 * scaling, 20 * scaling);
 	ImGui::PushStyleVar(ImGuiStyleVar_WindowPadding, padding);
 	ImGui::PushStyleVar(ImGuiStyleVar_ItemSpacing, padding);
-	if (ImGui::BeginPopupModal(analog ? "Map Axis" : "Map Button", NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
+	if (ImGui::BeginPopupModal(analog ? tr("Map Axis") : tr("Map Button"), NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
 	{
-		ImGui::Text("Waiting for %s '%s'...", analog ? "axis" : "button",
+		ImGui::Text(tr("Waiting for %s '%s'..."), analog ? "axis" : "button",
 				analog ? arcade_button_mode ? arcade_axis_names[index] : axis_names[index]
 						: arcade_button_mode ? arcade_button_names[index] : button_names[index]);
 		double now = os_GetSeconds();
-		ImGui::Text("Time out in %d s", (int)(5 - (now - map_start_time)));
+		ImGui::Text(tr("Time out in %d s"), (int)(5 - (now - map_start_time)));
 		if (mapped_code != (u32)-1)
 		{
 			std::shared_ptr<InputMapping> input_mapping = mapped_device->get_input_mapping();
@@ -675,10 +695,10 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 		const float col_width = (width
 				- ImGui::GetStyle().GrabMinSize
 				- (0 + ImGui::GetStyle().ItemSpacing.x)
-				- (ImGui::CalcTextSize("Map").x + ImGui::GetStyle().FramePadding.x * 2.0f + ImGui::GetStyle().ItemSpacing.x)) / 2;
+				- (ImGui::CalcTextSize(tr("Map")).x + ImGui::GetStyle().FramePadding.x * 2.0f + ImGui::GetStyle().ItemSpacing.x)) / 2;
 
 		std::shared_ptr<InputMapping> input_mapping = gamepad->get_input_mapping();
-		if (input_mapping == NULL || ImGui::Button("Done", ImVec2(100 * scaling, 30 * scaling)))
+		if (input_mapping == NULL || ImGui::Button(tr("Done"), ImVec2(100 * scaling, 30 * scaling)))
 		{
 			ImGui::CloseCurrentPopup();
 			gamepad->save_mapping();
@@ -690,12 +710,12 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 			ImGui::SameLine();
 			float w = ImGui::CalcItemWidth();
 			ImGui::PushItemWidth(w / 2);
-			if (ImGui::BeginCombo("Port", maple_ports[gamepad_port + 1]))
+			if (ImGui::BeginCombo(tr("Port"), maple_ports[gamepad_port + 1]))
 			{
 				for (u32 j = 0; j < MAPLE_PORTS; j++)
 				{
 					bool is_selected = gamepad_port == j;
-					if (ImGui::Selectable(maple_ports[j + 1], &is_selected))
+					if (ImGui::Selectable(tr(maple_ports[j + 1]), &is_selected))
 						gamepad_port = j;
 					if (is_selected)
 						ImGui::SetItemDefaultFocus();
@@ -704,9 +724,9 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 			}
 			ImGui::PopItemWidth();
 		}
-		ImGui::SameLine(ImGui::GetContentRegionAvail().x - ImGui::CalcTextSize("Arcade button names").x
+		ImGui::SameLine(ImGui::GetContentRegionAvail().x - ImGui::CalcTextSize(tr("Arcade button names")).x
 				- ImGui::GetStyle().FramePadding.x * 3.0f - ImGui::GetStyle().ItemSpacing.x);
-		ImGui::Checkbox("Arcade button names", &arcade_button_mode);
+		ImGui::Checkbox(tr("Arcade button names"), &arcade_button_mode);
 
 		char key_id[32];
 		ImGui::BeginGroup();
@@ -739,10 +759,10 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 					ImGui::Text("[%d]", code);
 			}
 			ImGui::NextColumn();
-			if (ImGui::Button("Map"))
+			if (ImGui::Button(tr("Map")))
 			{
 				map_start_time = os_GetSeconds();
-				ImGui::OpenPopup("Map Button");
+				ImGui::OpenPopup(tr("Map Button"));
 				mapped_device = gamepad;
 				mapped_code = -1;
 				gamepad->detect_btn_input([](u32 code)
@@ -789,10 +809,10 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 					ImGui::Text("[%d]", code);
 			}
 			ImGui::NextColumn();
-			if (ImGui::Button("Map"))
+			if (ImGui::Button(tr("Map")))
 			{
 				map_start_time = os_GetSeconds();
-				ImGui::OpenPopup("Map Axis");
+				ImGui::OpenPopup(tr("Map Axis"));
 				mapped_device = gamepad;
 				mapped_code = -1;
 				gamepad->detect_axis_input([](u32 code)
@@ -815,15 +835,15 @@ static void error_popup()
 {
 	if (!error_msg.empty())
 	{
-		ImGui::OpenPopup("Error");
-		if (ImGui::BeginPopupModal("Error", NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
+		ImGui::OpenPopup(tr("Error"));
+		if (ImGui::BeginPopupModal(tr("Error"), NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
 		{
 			ImGui::PushTextWrapPos(ImGui::GetCursorPos().x + 400.f * scaling);
 			ImGui::TextWrapped("%s", error_msg.c_str());
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, ImVec2(16 * scaling, 3 * scaling));
 			float currentwidth = ImGui::GetContentRegionAvail().x;
 			ImGui::SetCursorPosX((currentwidth - 80.f * scaling) / 2.f + ImGui::GetStyle().WindowPadding.x);
-			if (ImGui::Button("OK", ImVec2(80.f * scaling, 0.f)))
+			if (ImGui::Button(tr("OK"), ImVec2(80.f * scaling, 0.f)))
 			{
 				error_msg.clear();
 				ImGui::CloseCurrentPopup();
@@ -841,15 +861,15 @@ static void contentpath_warning_popup()
 
     if (scanner.content_path_looks_incorrect)
     {
-        ImGui::OpenPopup("Incorrect Content Location?");
-        if (ImGui::BeginPopupModal("Incorrect Content Location?", NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
+        ImGui::OpenPopup(tr("Incorrect Content Location?"));
+        if (ImGui::BeginPopupModal(tr("Incorrect Content Location?"), NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
         {
             ImGui::PushTextWrapPos(ImGui::GetCursorPos().x + 400.f * scaling);
             ImGui::TextWrapped("  Scanned %d folders but no game can be found!  ", scanner.empty_folders_scanned);
             ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, ImVec2(16 * scaling, 3 * scaling));
             float currentwidth = ImGui::GetContentRegionAvail().x;
             ImGui::SetCursorPosX((currentwidth - 100.f * scaling) / 2.f + ImGui::GetStyle().WindowPadding.x - 55.f * scaling);
-            if (ImGui::Button("Reselect", ImVec2(100.f * scaling, 0.f)))
+            if (ImGui::Button(tr("Reselect"), ImVec2(100.f * scaling, 0.f)))
             {
             	scanner.content_path_looks_incorrect = false;
                 ImGui::CloseCurrentPopup();
@@ -858,7 +878,7 @@ static void contentpath_warning_popup()
             
             ImGui::SameLine();
             ImGui::SetCursorPosX((currentwidth - 100.f * scaling) / 2.f + ImGui::GetStyle().WindowPadding.x + 55.f * scaling);
-            if (ImGui::Button("Cancel", ImVec2(100.f * scaling, 0.f)))
+            if (ImGui::Button(tr("Cancel"), ImVec2(100.f * scaling, 0.f)))
             {
             	scanner.content_path_looks_incorrect = false;
                 ImGui::CloseCurrentPopup();
@@ -900,7 +920,7 @@ static void gui_display_settings()
     ImGui::Begin("Settings", NULL, /*ImGuiWindowFlags_AlwaysAutoResize |*/ ImGuiWindowFlags_NoResize | ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoCollapse);
 	ImVec2 normal_padding = ImGui::GetStyle().FramePadding;
 
-    if (ImGui::Button("Done", ImVec2(100 * scaling, 30 * scaling)))
+    if (ImGui::Button(tr("Done"), ImVec2(100 * scaling, 30 * scaling)))
     {
     	if (game_started)
     		gui_state = GuiState::Commands;
@@ -923,7 +943,7 @@ static void gui_display_settings()
 		ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, ImVec2(16 * scaling, normal_padding.y));
 		if (config::Settings::instance().hasPerGameConfig())
 		{
-			if (ImGui::Button("Delete Game Config", ImVec2(0, 30 * scaling)))
+			if (ImGui::Button(tr("Delete Game Config"), ImVec2(0, 30 * scaling)))
 			{
 				config::Settings::instance().setPerGameConfig(false);
 				config::Settings::instance().load(false);
@@ -932,7 +952,7 @@ static void gui_display_settings()
 		}
 		else
 		{
-			if (ImGui::Button("Make Game Config", ImVec2(0, 30 * scaling)))
+			if (ImGui::Button(tr("Make Game Config"), ImVec2(0, 30 * scaling)))
 				config::Settings::instance().setPerGameConfig(true);
 		}
 	    ImGui::PopStyleVar();
@@ -942,19 +962,19 @@ static void gui_display_settings()
 
     if (ImGui::BeginTabBar("settings", ImGuiTabBarFlags_NoTooltip))
     {
-		if (ImGui::BeginTabItem("General"))
+		if (ImGui::BeginTabItem(tr("General")))
 		{
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, normal_padding);
 			const char *languages[] = { "Japanese", "English", "German", "French", "Spanish", "Italian", "Default" };
-			OptionComboBox("Language", config::Language, languages, ARRAY_SIZE(languages),
+			OptionComboBox(tr("Language"), config::Language, languages, ARRAY_SIZE(languages),
 				"The language as configured in the Dreamcast BIOS");
 
 			const char *broadcast[] = { "NTSC", "PAL", "PAL/M", "PAL/N", "Default" };
-			OptionComboBox("Broadcast", config::Broadcast, broadcast, ARRAY_SIZE(broadcast),
+			OptionComboBox(tr("Broadcast"), config::Broadcast, broadcast, ARRAY_SIZE(broadcast),
 					"TV broadcasting standard for non-VGA modes");
 
 			const char *region[] = { "Japan", "USA", "Europe", "Default" };
-			OptionComboBox("Region", config::Region, region, ARRAY_SIZE(region),
+			OptionComboBox(tr("Region"), config::Region, region, ARRAY_SIZE(region),
 						"BIOS region");
 
 			const char *cable[] = { "VGA", "RGB Component", "TV Composite" };
@@ -963,12 +983,12 @@ static void gui_display_settings()
 		        ImGui::PushItemFlag(ImGuiItemFlags_Disabled, true);
 		        ImGui::PushStyleVar(ImGuiStyleVar_Alpha, ImGui::GetStyle().Alpha * 0.5f);
 			}
-			if (ImGui::BeginCombo("Cable", cable[config::Cable == 0 ? 0 : config::Cable - 1], ImGuiComboFlags_None))
+			if (ImGui::BeginCombo(tr("Cable"), tr(cable[config::Cable == 0 ? 0 : config::Cable - 1]), ImGuiComboFlags_None))
 			{
 				for (int i = 0; i < IM_ARRAYSIZE(cable); i++)
 				{
 					bool is_selected = i == 0 ? config::Cable <= 1 : config::Cable - 1 == i;
-					if (ImGui::Selectable(cable[i], &is_selected))
+					if (ImGui::Selectable(tr(cable[i]), &is_selected))
 						config::Cable = i == 0 ? 0 : i + 1;
 	                if (is_selected)
 	                    ImGui::SetItemDefaultFocus();
@@ -988,7 +1008,7 @@ static void gui_display_settings()
             size.y = (ImGui::GetTextLineHeightWithSpacing() + ImGui::GetStyle().FramePadding.y * 2.f)
             				* (config::ContentPath.get().size() + 1) ;//+ ImGui::GetStyle().FramePadding.y * 2.f;
 
-            if (ImGui::ListBoxHeader("Content Location", size))
+            if (ImGui::ListBoxHeader(tr("Content Location"), size))
             {
             	int to_delete = -1;
                 for (u32 i = 0; i < config::ContentPath.get().size(); i++)
@@ -1002,7 +1022,7 @@ static void gui_display_settings()
                 	ImGui::PopID();
                 }
                 ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, ImVec2(24 * scaling, 3 * scaling));
-                if (ImGui::Button("Add"))
+                if (ImGui::Button(tr("Add")))
                 	ImGui::OpenPopup("Select Directory");
                 select_file_popup("Select Directory", [](bool cancelled, std::string selection)
                 		{
@@ -1027,7 +1047,7 @@ static void gui_display_settings()
             ShowHelpMarker("The directories where your games are stored");
 
 #ifdef __linux__
-            if (ImGui::ListBoxHeader("Data Directory", 1))
+            if (ImGui::ListBoxHeader(tr("Data Directory"), 1))
             {
             	ImGui::AlignTextToFramePadding();
                 ImGui::Text("%s", get_writable_data_path("").c_str());
@@ -1036,13 +1056,13 @@ static void gui_display_settings()
             ImGui::SameLine();
             ShowHelpMarker("The directory containing BIOS files, as well as saved VMUs and states");
 #else
-            if (ImGui::ListBoxHeader("Home Directory", 1))
+            if (ImGui::ListBoxHeader(tr("Home Directory"), 1))
             {
             	ImGui::AlignTextToFramePadding();
                 ImGui::Text("%s", get_writable_config_path("").c_str());
 #ifdef __ANDROID__
                 ImGui::SameLine(ImGui::GetContentRegionAvail().x - ImGui::CalcTextSize("Change").x - ImGui::GetStyle().FramePadding.x);
-                if (ImGui::Button("Change"))
+                if (ImGui::Button(tr("Change")))
                 	gui_state = GuiState::Onboarding;
 #endif
                 ImGui::ListBoxFooter();
@@ -1050,19 +1070,19 @@ static void gui_display_settings()
             ImGui::SameLine();
             ShowHelpMarker("The directory where Flycast saves configuration files and VMUs. BIOS files should be in a subfolder named \"data\"");
 #endif
-			if (OptionCheckbox("Hide Legacy Naomi Roms", config::HideLegacyNaomiRoms,
+			if (OptionCheckbox(tr("Hide Legacy Naomi Roms"), config::HideLegacyNaomiRoms,
 					"Hide .bin, .dat and .lst files from the content browser"))
 				scanner.refresh();
-			OptionCheckbox("Auto load/save state", config::AutoSavestate,
+			OptionCheckbox(tr("Auto load/save state"), config::AutoSavestate,
 					"Automatically save the state of the game when stopping and load it at start up.");
 
 			ImGui::PopStyleVar();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem("Controls"))
+		if (ImGui::BeginTabItem(tr("Controls")))
 		{
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, normal_padding);
-		    if (ImGui::CollapsingHeader("Dreamcast Devices", ImGuiTreeNodeFlags_DefaultOpen))
+		    if (ImGui::CollapsingHeader(tr("Dreamcast Devices"), ImGuiTreeNodeFlags_DefaultOpen))
 		    {
 				for (int bus = 0; bus < MAPLE_PORTS; bus++)
 				{
@@ -1077,7 +1097,7 @@ static void gui_display_settings()
 						for (int i = 0; i < IM_ARRAYSIZE(maple_device_types); i++)
 						{
 							bool is_selected = config::MapleMainDevices[bus] == maple_device_type_from_index(i);
-							if (ImGui::Selectable(maple_device_types[i], &is_selected))
+							if (ImGui::Selectable(tr(maple_device_types[i]), &is_selected))
 							{
 								config::MapleMainDevices[bus] = maple_device_type_from_index(i);
 								maple_devices_changed = true;
@@ -1100,7 +1120,7 @@ static void gui_display_settings()
 							for (int i = 0; i < IM_ARRAYSIZE(maple_expansion_device_types); i++)
 							{
 								bool is_selected = config::MapleExpansionDevices[bus][port] == maple_expansion_device_type_from_index(i);
-								if (ImGui::Selectable(maple_expansion_device_types[i], &is_selected))
+								if (ImGui::Selectable(tr(maple_expansion_device_types[i]), &is_selected))
 								{
 									config::MapleExpansionDevices[bus][port] = maple_expansion_device_type_from_index(i);
 									maple_devices_changed = true;
@@ -1128,7 +1148,7 @@ static void gui_display_settings()
 								| ImGuiColorEditFlags_NoInputs | ImGuiColorEditFlags_NoTooltip | ImGuiColorEditFlags_NoLabel);
 						ImGui::SameLine();
 						bool enabled = color != 0;
-						ImGui::Checkbox("Crosshair", &enabled);
+						ImGui::Checkbox(tr("Crosshair"), &enabled);
 						if (enabled)
 						{
 							config::CrosshairColor[bus] = (u8)(xhairColor[0] * 255.f)
@@ -1148,7 +1168,7 @@ static void gui_display_settings()
 				}
 				ImGui::Spacing();
 		    }
-		    if (ImGui::CollapsingHeader("Physical Devices", ImGuiTreeNodeFlags_DefaultOpen))
+		    if (ImGui::CollapsingHeader(tr("Physical Devices"), ImGuiTreeNodeFlags_DefaultOpen))
 		    {
 				ImGui::Columns(4, "physicalDevices", false);
 				ImGui::Text("System");
@@ -1178,7 +1198,7 @@ static void gui_display_settings()
 						for (int j = -1; j < (int)ARRAY_SIZE(maple_ports) - 1; j++)
 						{
 							bool is_selected = gamepad->maple_port() == j;
-							if (ImGui::Selectable(maple_ports[j + 1], &is_selected))
+							if (ImGui::Selectable(tr(maple_ports[j + 1]), &is_selected))
 								gamepad->set_maple_port(j);
 							if (is_selected)
 								ImGui::SetItemDefaultFocus();
@@ -1187,7 +1207,7 @@ static void gui_display_settings()
 						ImGui::EndCombo();
 					}
 					ImGui::NextColumn();
-					if (gamepad->remappable() && ImGui::Button("Map"))
+					if (gamepad->remappable() && ImGui::Button(tr("Map")))
 					{
 						gamepad_port = 0;
 						ImGui::OpenPopup("Controller Mapping");
@@ -1219,7 +1239,7 @@ static void gui_display_settings()
 			ImGui::PopStyleVar();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem("Video"))
+		if (ImGui::BeginTabItem(tr("Video")))
 		{
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, normal_padding);
 #if !defined(__APPLE__)
@@ -1237,17 +1257,17 @@ static void gui_display_settings()
 		    {
 		    	int renderer = (pvr_rend == RenderType::OpenGL_OIT || pvr_rend == RenderType::Vulkan_OIT) ? 2 : config::PerStripSorting ? 1 : 0;
 		    	ImGui::Columns(has_per_pixel ? 3 : 2, "renderers", false);
-		    	ImGui::RadioButton("Per Triangle", &renderer, 0);
+		    	ImGui::RadioButton(tr("Per Triangle"), &renderer, 0);
 	            ImGui::SameLine();
 	            ShowHelpMarker("Sort transparent polygons per triangle. Fast but may produce graphical glitches");
             	ImGui::NextColumn();
-		    	ImGui::RadioButton("Per Strip", &renderer, 1);
+		    	ImGui::RadioButton(tr("Per Strip"), &renderer, 1);
 	            ImGui::SameLine();
 	            ShowHelpMarker("Sort transparent polygons per strip. Faster but may produce graphical glitches");
 	            if (has_per_pixel)
 	            {
 	            	ImGui::NextColumn();
-	            	ImGui::RadioButton("Per Pixel", &renderer, 2);
+	            	ImGui::RadioButton(tr("Per Pixel"), &renderer, 2);
 	            	ImGui::SameLine();
 	            	ShowHelpMarker("Sort transparent polygons per pixel. Slower but accurate");
 	            }
@@ -1276,21 +1296,21 @@ static void gui_display_settings()
 		    		break;
 		    	}
 		    }
-		    if (ImGui::CollapsingHeader("Rendering Options", ImGuiTreeNodeFlags_DefaultOpen))
+		    if (ImGui::CollapsingHeader(tr("Rendering Options"), ImGuiTreeNodeFlags_DefaultOpen))
 		    {
-		    	ImGui::Text("Automatic Frame Skipping:");
+		    	ImGui::Text(tr("Automatic Frame Skipping:"));
 		    	ImGui::Columns(3, "autoskip", false);
 		    	OptionRadioButton("Disabled", config::AutoSkipFrame, 0, "No frame skipping");
             	ImGui::NextColumn();
-		    	OptionRadioButton("Normal", config::AutoSkipFrame, 1, "Skip a frame when the GPU and CPU are both running slow");
+		    	OptionRadioButton(tr("Normal"), config::AutoSkipFrame, 1, "Skip a frame when the GPU and CPU are both running slow");
             	ImGui::NextColumn();
-		    	OptionRadioButton("Maximum", config::AutoSkipFrame, 2, "Skip a frame when the GPU is running slow");
+		    	OptionRadioButton(tr("Maximum"), config::AutoSkipFrame, 2, "Skip a frame when the GPU is running slow");
 		    	ImGui::Columns(1, nullptr, false);
 
-		    	OptionCheckbox("Shadows", config::ModifierVolumes,
+		    	OptionCheckbox(tr("Shadows"), config::ModifierVolumes,
 		    			"Enable modifier volumes, usually used for shadows");
-		    	OptionCheckbox("Fog", config::Fog, "Enable fog effects");
-		    	OptionCheckbox("Widescreen", config::Widescreen,
+		    	OptionCheckbox(tr("Fog"), config::Fog, "Enable fog effects");
+		    	OptionCheckbox(tr("Widescreen"), config::Widescreen,
 		    			"Draw geometry outside of the normal 4:3 aspect ratio. May produce graphical glitches in the revealed areas");
 		    	if (!config::Widescreen)
 		    	{
@@ -1298,7 +1318,7 @@ static void gui_display_settings()
 			        ImGui::PushStyleVar(ImGuiStyleVar_Alpha, ImGui::GetStyle().Alpha * 0.5f);
 		    	}
 		    	ImGui::Indent();
-		    	OptionCheckbox("Super Widescreen", config::SuperWidescreen,
+		    	OptionCheckbox(tr("Super Widescreen"), config::SuperWidescreen,
 		    			"Use the full width of the screen or window when its aspect ratio is greater than 16:9");
 		    	ImGui::Unindent();
 		    	if (!config::Widescreen)
@@ -1306,15 +1326,15 @@ static void gui_display_settings()
 			        ImGui::PopItemFlag();
 			        ImGui::PopStyleVar();
 		    	}
-		    	OptionCheckbox("Widescreen Game Cheats", config::WidescreenGameHacks,
+		    	OptionCheckbox(tr("Widescreen Game Cheats"), config::WidescreenGameHacks,
 		    			"Modify the game so that it displays in 16:9 anamorphic format and use horizontal screen stretching. Only some games are supported.");
 #ifndef __APPLE__
-		    	OptionCheckbox("VSync", config::VSync, "Synchronizes the frame rate with the screen refresh rate. Recommended");
+		    	OptionCheckbox(tr("VSync"), config::VSync, "Synchronizes the frame rate with the screen refresh rate. Recommended");
 #endif
-		    	OptionCheckbox("Show FPS Counter", config::ShowFPS, "Show on-screen frame/sec counter");
-		    	OptionCheckbox("Show VMU In-game", config::FloatVMUs, "Show the VMU LCD screens while in-game");
-		    	OptionCheckbox("Rotate Screen 90°", config::Rotate90, "Rotate the screen 90° counterclockwise");
-		    	OptionCheckbox("Delay Frame Swapping", config::DelayFrameSwapping,
+		    	OptionCheckbox(tr("Show FPS Counter"), config::ShowFPS, "Show on-screen frame/sec counter");
+		    	OptionCheckbox(tr("Show VMU In-game"), config::FloatVMUs, "Show the VMU LCD screens while in-game");
+		    	OptionCheckbox(tr("Rotate Screen 90°"), config::Rotate90, "Rotate the screen 90° counterclockwise");
+		    	OptionCheckbox(tr("Delay Frame Swapping"), config::DelayFrameSwapping,
 		    			"Useful to avoid flashing screen or glitchy videos. Not recommended on slow platforms");
 #ifdef USE_VULKAN
 				ImGui::Checkbox("Use Vulkan Renderer", &vulkan);
@@ -1369,7 +1389,7 @@ static void gui_display_settings()
                 }
                 ImGui::SameLine(0, scaling_style.ItemInnerSpacing.x);
                 
-                ImGui::Text("Internal Resolution");
+                ImGui::Text(tr("Internal Resolution"));
                 ImGui::SameLine();
                 ShowHelpMarker("Internal render resolution. Higher is better but more demanding");
 
@@ -1393,17 +1413,17 @@ static void gui_display_settings()
 		    	OptionSlider("Max Threads", config::MaxThreads, 1, 8,
 		    			"Maximum number of threads to use for texture upscaling. Recommended: number of physical cores minus one");
 #endif
-		    	OptionCheckbox("Load Custom Textures", config::CustomTextures,
+		    	OptionCheckbox(tr("Load Custom Textures"), config::CustomTextures,
 		    			"Load custom/high-res textures from data/textures/<game id>");
 		    }
 			ImGui::PopStyleVar();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem("Audio"))
+		if (ImGui::BeginTabItem(tr("Audio")))
 		{
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, normal_padding);
 			OptionCheckbox("Disable Sound", config::DisableSound, "Disable the emulator sound output");
-			OptionCheckbox("Enable DSP", config::DSPEnabled,
+			OptionCheckbox(tr("Enable DSP"), config::DSPEnabled,
 					"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms");
 #ifdef __ANDROID__
 			if (config::AudioBackend.get() == "auto" || config::AudioBackend.get() == "android")
@@ -1414,7 +1434,7 @@ static void gui_display_settings()
             		|| (config::AudioBackend.get() != "auto" && config::AudioBackend.get() != "android"))
             {
 				int latency = (int)roundf(config::AudioBufferSize * 1000.f / 44100.f);
-				ImGui::SliderInt("Latency", &latency, 12, 512, "%d ms");
+				ImGui::SliderInt(tr("Latency"), &latency, 12, 512, "%d ms");
 				config::AudioBufferSize = (int)roundf(latency * 44100.f / 1000.f);
 				ImGui::SameLine();
 				ShowHelpMarker("Sets the maximum audio latency. Not supported by all audio drivers.");
@@ -1508,16 +1528,16 @@ static void gui_display_settings()
 			ImGui::PopStyleVar();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem("Advanced"))
+		if (ImGui::BeginTabItem(tr("Advanced")))
 		{
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, normal_padding);
 		    if (ImGui::CollapsingHeader("CPU Mode", ImGuiTreeNodeFlags_DefaultOpen))
 		    {
 				ImGui::Columns(2, "cpu_modes", false);
-				OptionRadioButton("Dynarec", config::DynarecEnabled, true,
+				OptionRadioButton(tr("Dynarec"), config::DynarecEnabled, true,
 					"Use the dynamic recompiler. Recommended in most cases");
 				ImGui::NextColumn();
-				OptionRadioButton("Interpreter", config::DynarecEnabled, false,
+				OptionRadioButton(tr("Interpreter"), config::DynarecEnabled, false,
 					"Use the interpreter. Very slow but may help in case of a dynarec problem");
 				ImGui::Columns(1, NULL, false);
 		    }
@@ -1525,13 +1545,13 @@ static void gui_display_settings()
 		    {
 		    	OptionCheckbox("Safe Mode", config::DynarecSafeMode,
 		    			"Do not optimize integer division. Not recommended");
-		    	OptionCheckbox("Idle Skip", config::DynarecIdleSkip, "Skip wait loops. Recommended");
+		    	OptionCheckbox(tr("Idle Skip"), config::DynarecIdleSkip, "Skip wait loops. Recommended");
 		    }
 		    if (ImGui::CollapsingHeader("Network", ImGuiTreeNodeFlags_DefaultOpen))
 		    {
-		    	OptionCheckbox("Broadband Adapter Emulation", config::EmulateBBA,
+		    	OptionCheckbox(tr("Broadband Adapter Emulation"), config::EmulateBBA,
 		    			"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem");
-		    	OptionCheckbox("Enable Naomi Networking", config::NetworkEnable,
+		    	OptionCheckbox(tr("Enable Naomi Networking"), config::NetworkEnable,
 		    			"Enable networking for supported Naomi games");
 		    	if (config::NetworkEnable)
 		    	{
@@ -1550,19 +1570,19 @@ static void gui_display_settings()
 		    }
 		    if (ImGui::CollapsingHeader("Other", ImGuiTreeNodeFlags_DefaultOpen))
 		    {
-		    	OptionCheckbox("HLE BIOS", config::UseReios, "Force high-level BIOS emulation");
-	            OptionCheckbox("Force Windows CE", config::ForceWindowsCE,
+		    	OptionCheckbox(tr("HLE BIOS"), config::UseReios, "Force high-level BIOS emulation");
+	            OptionCheckbox(tr("Force Windows CE"), config::ForceWindowsCE,
 	            		"Enable full MMU emulation and other Windows CE settings. Do not enable unless necessary");
 #ifndef __ANDROID
-	            OptionCheckbox("Serial Console", config::SerialConsole,
+	            OptionCheckbox(tr("Serial Console"), config::SerialConsole,
 	            		"Dump the Dreamcast serial console to stdout");
 #endif
-	            OptionCheckbox("Dump Textures", config::DumpTextures,
+	            OptionCheckbox(tr("Dump Textures"), config::DumpTextures,
 	            		"Dump all textures into data/texdump/<game id>");
 
 	            bool logToFile = cfgLoadBool("log", "LogToFile", false);
 	            bool newLogToFile = logToFile;
-				ImGui::Checkbox("Log to File", &newLogToFile);
+				ImGui::Checkbox(tr("Log to File"), &newLogToFile);
 				if (logToFile != newLogToFile)
 				{
 					cfgSaveBool("log", "LogToFile", newLogToFile);
@@ -1575,18 +1595,18 @@ static void gui_display_settings()
 			ImGui::PopStyleVar();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem("About"))
+		if (ImGui::BeginTabItem(tr("About")))
 		{
 			ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, normal_padding);
 		    if (ImGui::CollapsingHeader("Flycast", ImGuiTreeNodeFlags_DefaultOpen))
 		    {
-				ImGui::Text("Version: %s", REICAST_VERSION);
+				ImGui::Text(tr("Version: %s"), REICAST_VERSION);
 				ImGui::Text("Git Hash: %s", GIT_HASH);
 				ImGui::Text("Build Date: %s", BUILD_DATE);
 		    }
 		    if (ImGui::CollapsingHeader("Platform", ImGuiTreeNodeFlags_DefaultOpen))
 		    {
-		    	ImGui::Text("CPU: %s",
+		    	ImGui::Text(tr("CPU: %s"),
 #if HOST_CPU == CPU_X86
 					"x86"
 #elif HOST_CPU == CPU_ARM
@@ -1626,7 +1646,7 @@ static void gui_display_settings()
 				if (ImGui::CollapsingHeader("Open GL", ImGuiTreeNodeFlags_DefaultOpen))
 				{
 		    		ImGui::Text("Renderer: %s", (const char *)glGetString(GL_RENDERER));
-		    		ImGui::Text("Version: %s", (const char *)glGetString(GL_VERSION));
+		    		ImGui::Text(tr("Version: %s"), (const char *)glGetString(GL_VERSION));
 		    	}
 	    	}
 #ifdef USE_VULKAN
@@ -1637,7 +1657,7 @@ static void gui_display_settings()
 		    		std::string name = VulkanContext::Instance()->GetDriverName();
 		    		ImGui::Text("Driver Name: %s", name.c_str());
 		    		std::string version = VulkanContext::Instance()->GetDriverVersion();
-		    		ImGui::Text("Version: %s", version.c_str());
+		    		ImGui::Text(tr("Version: %s"), version.c_str());
 				}
 	    	}
 #endif
@@ -1699,7 +1719,7 @@ static void gui_display_content()
     ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, ImVec2(20 * scaling, 8 * scaling));		// from 8, 4
     ImGui::AlignTextToFramePadding();
     ImGui::Indent(10 * scaling);
-    ImGui::Text("GAMES");
+    ImGui::Text(tr("GAMES"));
     ImGui::Unindent(10 * scaling);
 
     static ImGuiTextFilter filter;
@@ -1711,7 +1731,7 @@ static void gui_display_content()
     if (gui_state != GuiState::SelectDisk)
     {
 		ImGui::SameLine(ImGui::GetContentRegionMax().x - ImGui::CalcTextSize("Settings").x - ImGui::GetStyle().FramePadding.x * 2.0f);
-		if (ImGui::Button("Settings"))
+		if (ImGui::Button(tr("Settings")))
 			gui_state = GuiState::Settings;
     }
     ImGui::PopStyleVar();
@@ -1846,7 +1866,7 @@ static void gui_network_start()
 		if (networkStatus.get())
 		{
 			gui_state = GuiState::Closed;
-			ImGui::Text("STARTING...");
+			ImGui::Text(tr("STARTING..."));
 		}
 		else
 		{
@@ -1856,7 +1876,7 @@ static void gui_network_start()
 	}
 	else
 	{
-		ImGui::Text("STARTING NETWORK...");
+		ImGui::Text(tr("STARTING NETWORK..."));
 		if (config::ActAsServer)
 			ImGui::Text("Press Start to start the game now.");
 	}
@@ -1865,7 +1885,7 @@ static void gui_network_start()
 	float currentwidth = ImGui::GetContentRegionAvail().x;
 	ImGui::SetCursorPosX((currentwidth - 100.f * scaling) / 2.f + ImGui::GetStyle().WindowPadding.x);
 	ImGui::SetCursorPosY(126.f * scaling);
-	if (ImGui::Button("Cancel", ImVec2(100.f * scaling, 0.f)))
+	if (ImGui::Button(tr("Cancel"), ImVec2(100.f * scaling, 0.f)))
 	{
 		naomiNetwork.terminate();
 		networkStatus.get();
@@ -1901,7 +1921,7 @@ static void gui_display_loadscreen()
 			else
 			{
 				gui_state = GuiState::Closed;
-				ImGui::Text("STARTING...");
+				ImGui::Text(tr("STARTING..."));
 			}
 		} catch (const ReicastException& ex) {
 			ERROR_LOG(BOOT, "%s", ex.reason.c_str());
@@ -1915,14 +1935,14 @@ static void gui_display_loadscreen()
 	}
 	else
 	{
-		ImGui::Text("LOADING... ");
+		ImGui::Text(tr("LOADING... "));
 		ImGui::SameLine();
 		ImGui::Text("%s", get_notification().c_str());
 
 		float currentwidth = ImGui::GetContentRegionAvail().x;
 		ImGui::SetCursorPosX((currentwidth - 100.f * scaling) / 2.f + ImGui::GetStyle().WindowPadding.x);
 		ImGui::SetCursorPosY(126.f * scaling);
-		if (ImGui::Button("Cancel", ImVec2(100.f * scaling, 0.f)))
+		if (ImGui::Button(tr("Cancel"), ImVec2(100.f * scaling, 0.f)))
 		{
 			dc_cancel_load();
 			gui_state = GuiState::Main;
diff --git a/core/rend/i18n.h b/core/rend/i18n.h
new file mode 100644
index 000000000..5dd92f26e
--- /dev/null
+++ b/core/rend/i18n.h
@@ -0,0 +1,427 @@
+#pragma once
+#include <cstring>
+#include <unordered_map>
+
+struct LangEntry {
+    const char* en;
+    const char* zh;
+};
+
+static const std::unordered_map<std::string, LangEntry> translations = {
+    // 主菜单
+    {"Resume", {"Resume", "继续"}},
+    {"Settings", {"Settings", "设置"}},
+    {"Cheats", {"Cheats", "金手指"}},
+    {"Exit", {"Exit", "退出"}},
+    {"Load State", {"Load State", "加载存档"}},
+    {"Save State", {"Save State", "保存存档"}},
+    {"Close Game", {"Close Game", "关闭游戏"}},
+    {"Slot ", {"Slot ", "存档槽 "}},
+    
+    // 设置标签
+    {"General", {"General", "通用"}},
+    {"Video", {"Video", "视频"}},
+    {"Audio", {"Audio", "音频"}},
+    {"Controls", {"Controls", "控制"}},
+    {"Advanced", {"Advanced", "高级"}},
+    {"About", {"About", "关于"}},
+    {"Network", {"Network", "网络"}},
+    
+    // 按钮
+    {"Done", {"Done", "完成"}},
+    {"Cancel", {"Cancel", "取消"}},
+    {"OK", {"OK", "确定"}},
+    {"Yes", {"Yes", "是"}},
+    {"No", {"No", "否"}},
+    {"Map", {"Map", "映射"}},
+    {"Unmap", {"Unmap", "取消映射"}},
+    {"Add", {"Add", "添加"}},
+    {"Change", {"Change", "更改"}},
+    {"Edit", {"Edit", "编辑"}},
+    {"Delete", {"Delete", "删除"}},
+    {"Reset...", {"Reset...", "重置..."}},
+    {"Close", {"Close", "关闭"}},
+    {"Start", {"Start", "开始"}},
+    {"Stop", {"Stop", "停止"}},
+    
+    // 通用设置
+    {"Region", {"Region", "区域"}},
+    {"Language", {"Language", "语言"}},
+    {"Broadcast", {"Broadcast", "广播制式"}},
+    {"Cable", {"Cable", "线缆类型"}},
+    {"Default", {"Default", "默认"}},
+    {"Disabled", {"Disabled", "禁用"}},
+    {"Enabled", {"Enabled", "启用"}},
+    {"Normal", {"Normal", "正常"}},
+    {"Maximum", {"Maximum", "最大"}},
+    {"None", {"None", "无"}},
+    {"System", {"System", "系统"}},
+    {"Content Location", {"Content Location", "内容位置"}},
+    {"Hide Legacy Naomi Roms", {"Hide Legacy Naomi Roms", "隐藏旧版 Naomi ROM"}},
+    {"Auto load/save state", {"Auto load/save state", "自动加载/保存存档"}},
+    {"Automatically save the state of the game when stopping and load it at start up.", 
+     {"Automatically save the state of the game when stopping and load it at start up.", 
+      "停止时自动保存游戏状态，启动时自动加载。"}},
+    {"Naomi Free Play", {"Naomi Free Play", "Naomi 免费游戏"}},
+    {"BIOS region", {"BIOS region", "BIOS 区域"}},
+    {"Dreamcast BIOS", {"Dreamcast BIOS", "Dreamcast BIOS"}},
+    
+    // 视频设置
+    {"Shadows", {"Shadows", "阴影"}},
+    {"Fog", {"Fog", "雾效"}},
+    {"Widescreen", {"Widescreen", "宽屏"}},
+    {"Super Widescreen", {"Super Widescreen", "超宽屏"}},
+    {"VSync", {"VSync", "垂直同步"}},
+    {"Fullscreen", {"Fullscreen", "全屏"}},
+    {"Internal Resolution", {"Internal Resolution", "内部分辨率"}},
+    {"Horizontal Stretching", {"Horizontal Stretching", "水平拉伸"}},
+    {"Frame Skipping", {"Frame Skipping", "跳帧"}},
+    {"Frame Delay", {"Frame Delay", "帧延迟"}},
+    {"Anisotropic Filtering", {"Anisotropic Filtering", "各向异性过滤"}},
+    {"Transparent Sorting", {"Transparent Sorting", "透明排序"}},
+    {"Per Triangle", {"Per Triangle", "逐三角形"}},
+    {"Per Strip", {"Per Strip", "逐条带"}},
+    {"Per Pixel", {"Per Pixel", "逐像素"}},
+    {"Rendering Options", {"Rendering Options", "渲染选项"}},
+    {"Render to Texture", {"Render to Texture", "渲染到纹理"}},
+    {"Copy to VRAM", {"Copy to VRAM", "复制到 VRAM"}},
+    {"Texture Upscaling", {"Texture Upscaling", "纹理放大"}},
+    {"Show FPS Counter", {"Show FPS Counter", "显示 FPS 计数器"}},
+    {"Show VMU In-game", {"Show VMU In-game", "游戏内显示 VMU"}},
+    {"Delay Frame Swapping", {"Delay Frame Swapping", "延迟帧交换"}},
+    {"Dump Textures", {"Dump Textures", "转储纹理"}},
+    {"Load Custom Textures", {"Load Custom Textures", "加载自定义纹理"}},
+    {"Rotate Screen 90°", {"Rotate Screen 90°", "旋转屏幕 90°"}},
+    {"Native Depth Interpolation", {"Native Depth Interpolation", "原生深度插值"}},
+    {"Duplicate frames", {"Duplicate frames", "重复帧"}},
+    {"Widescreen Game Cheats", {"Widescreen Game Cheats", "宽屏游戏金手指"}},
+    {"Automatic Frame Skipping:", {"Automatic Frame Skipping:", "自动跳帧："}},
+    {"No frame skipping", {"No frame skipping", "不跳帧"}},
+    {"Per Pixel Settings", {"Per Pixel Settings", "逐像素设置"}},
+    {"Pixel Buffer Size", {"Pixel Buffer Size", "像素缓冲区大小"}},
+    {"Maximum Layers", {"Maximum Layers", "最大图层数"}},
+    {"Texture Max Size", {"Texture Max Size", "纹理最大尺寸"}},
+    {"Max Threads", {"Max Threads", "最大线程数"}},
+    {"Upscaled Texture Max Size", {"Upscaled Texture Max Size", "放大纹理最大尺寸"}},
+    {"Crosshair color", {"Crosshair color", "准星颜色"}},
+    {"Crosshair", {"Crosshair", "准星"}},
+    {"Video connection type", {"Video connection type", "视频连接类型"}},
+    {"Integer Scaling", {"Integer Scaling", "整数缩放"}},
+    {"Aspect Ratio", {"Aspect Ratio", "宽高比"}},
+    {"Graphics API", {"Graphics API", "图形 API"}},
+    
+    // 描述文本
+    {"Sort transparent polygons per triangle. Fast but may produce graphical glitches",
+     {"Sort transparent polygons per triangle. Fast but may produce graphical glitches",
+      "逐三角形排序透明多边形。速度快但可能产生图形故障"}},
+    {"Sort transparent polygons per strip. Faster but may produce graphical glitches",
+     {"Sort transparent polygons per strip. Faster but may produce graphical glitches",
+      "逐条带排序透明多边形。较快但可能产生图形故障"}},
+    {"Sort transparent polygons per pixel. Slower but accurate",
+     {"Sort transparent polygons per pixel. Slower but accurate",
+      "逐像素排序透明多边形。较慢但精确"}},
+    {"Draw geometry outside of the normal 4:3 aspect ratio. May produce graphical glitches in the revealed areas",
+     {"Draw geometry outside of the normal 4:3 aspect ratio. May produce graphical glitches in the revealed areas",
+      "在正常 4:3 比例外绘制几何体。可能在显示区域产生图形故障"}},
+    {"Copy rendered-to textures back to VRAM. Slower but accurate",
+     {"Copy rendered-to textures back to VRAM. Slower but accurate",
+      "将渲染纹理复制回 VRAM。较慢但精确"}},
+    {"Enable modifier volumes, usually used for shadows",
+     {"Enable modifier volumes, usually used for shadows",
+      "启用修改器体积，通常用于阴影"}},
+    {"Enable fog effects", {"Enable fog effects", "启用雾效"}},
+    {"Show on-screen frame/sec counter", {"Show on-screen frame/sec counter", "显示帧率计数器"}},
+    {"Show the VMU LCD screens while in-game", {"Show the VMU LCD screens while in-game", "游戏时显示 VMU LCD 屏幕"}},
+    {"Useful to avoid flashing screen or glitchy videos. Not recommended on slow platforms",
+     {"Useful to avoid flashing screen or glitchy videos. Not recommended on slow platforms",
+      "有助于避免闪屏或视频故障。不建议在慢速平台上使用"}},
+    {"Upscale textures with the xBRZ algorithm. Only on fast platforms and for certain 2D games",
+     {"Upscale textures with the xBRZ algorithm. Only on fast platforms and for certain 2D games",
+      "使用 xBRZ 算法放大纹理。仅适用于快速平台和某些 2D 游戏"}},
+    {"Textures larger than this dimension squared will not be upscaled",
+     {"Textures larger than this dimension squared will not be upscaled",
+      "大于此尺寸平方的纹理将不会被放大"}},
+    {"Synchronizes the frame rate with the screen refresh rate. Recommended",
+     {"Synchronizes the frame rate with the screen refresh rate. Recommended",
+      "将帧率与屏幕刷新率同步。推荐"}},
+    {"Stretch the screen horizontally", {"Stretch the screen horizontally", "水平拉伸屏幕"}},
+    
+    // 音频设置
+    {"Volume Level", {"Volume Level", "音量"}},
+    {"Enable DSP", {"Enable DSP", "启用 DSP"}},
+    {"Latency", {"Latency", "延迟"}},
+    {"Audio Driver", {"Audio Driver", "音频驱动"}},
+    {"Automatic Latency", {"Automatic Latency", "自动延迟"}},
+    {"Automatically set audio latency. Recommended",
+     {"Automatically set audio latency. Recommended",
+      "自动设置音频延迟。推荐"}},
+    {"Disable Sound", {"Disable Sound", "禁用声音"}},
+    {"Disable the emulator sound output", {"Disable the emulator sound output", "禁用模拟器声音输出"}},
+    {"The audio driver to use", {"The audio driver to use", "要使用的音频驱动"}},
+    {"Sets the maximum audio latency. Not supported by all audio drivers.",
+     {"Sets the maximum audio latency. Not supported by all audio drivers.",
+      "设置最大音频延迟。并非所有音频驱动都支持。"}},
+    
+    // 控制设置
+    {"Controller Mapping", {"Controller Mapping", "控制器映射"}},
+    {"Controller Type:", {"Controller Type:", "控制器类型："}},
+    {"Physical Devices", {"Physical Devices", "物理设备"}},
+    {"Dreamcast Devices", {"Dreamcast Devices", "Dreamcast 设备"}},
+    {"Mouse sensitivity", {"Mouse sensitivity", "鼠标灵敏度"}},
+    {"Use Raw Input", {"Use Raw Input", "使用原始输入"}},
+    {"Haptic", {"Haptic", "触觉"}},
+    {"Arcade button names", {"Arcade button names", "街机按钮名称"}},
+    {"Confirm Reset", {"Confirm Reset", "确认重置"}},
+    {"Delete Game Config", {"Delete Game Config", "删除游戏配置"}},
+    {"Make Game Config", {"Make Game Config", "创建游戏配置"}},
+    {"Waiting for control '%s'...", {"Waiting for control '%s'...", "等待控制 '%s'..."}},
+    {"Time out in %d s", {"Time out in %d s", "超时 %d 秒"}},
+    {"Map Control", {"Map Control", "映射控制"}},
+    {"Are you sure you want to reset the mappings to default?",
+     {"Are you sure you want to reset the mappings to default?",
+      "确定要将映射重置为默认值吗？"}},
+    
+    // 按钮名称
+    {"Button 1", {"Button 1", "按钮 1"}},
+    {"Button 2", {"Button 2", "按钮 2"}},
+    {"Button 3", {"Button 3", "按钮 3"}},
+    {"Button 4", {"Button 4", "按钮 4"}},
+    {"Button 5", {"Button 5", "按钮 5"}},
+    {"Button 6", {"Button 6", "按钮 6"}},
+    {"Button 7", {"Button 7", "按钮 7"}},
+    {"Button 8", {"Button 8", "按钮 8"}},
+    {"Coin", {"Coin", "投币"}},
+    {"Service", {"Service", "服务"}},
+    {"Test", {"Test", "测试"}},
+    {"Start", {"Start", "开始"}},
+    {"Up", {"Up", "上"}},
+    {"Down", {"Down", "下"}},
+    {"Left", {"Left", "左"}},
+    {"Right", {"Right", "右"}},
+    {"DPad Up", {"DPad Up", "方向键上"}},
+    {"DPad Down", {"DPad Down", "方向键下"}},
+    {"DPad Left", {"DPad Left", "方向键左"}},
+    {"DPad Right", {"DPad Right", "方向键右"}},
+    {"Ascii Stick", {"Ascii Stick", "Ascii 摇杆"}},
+    {"Twin Stick", {"Twin Stick", "双摇杆"}},
+    {"Rumble", {"Rumble", "震动"}},
+    
+    // 高级设置
+    {"CPU Mode", {"CPU Mode", "CPU 模式"}},
+    {"Dynarec", {"Dynarec", "动态重编译"}},
+    {"Interpreter", {"Interpreter", "解释器"}},
+    {"Dynarec Options", {"Dynarec Options", "动态重编译选项"}},
+    {"Idle Skip", {"Idle Skip", "跳过空闲"}},
+    {"HLE BIOS", {"HLE BIOS", "HLE BIOS"}},
+    {"Force Windows CE", {"Force Windows CE", "强制 Windows CE"}},
+    {"Multi-threaded emulation", {"Multi-threaded emulation", "多线程模拟"}},
+    {"Serial Console", {"Serial Console", "串口控制台"}},
+    {"Skip wait loops. Recommended", {"Skip wait loops. Recommended", "跳过等待循环。推荐"}},
+    {"Force high-level BIOS emulation", {"Force high-level BIOS emulation", "强制高级 BIOS 模拟"}},
+    {"Enable full MMU emulation and other Windows CE settings. Do not enable unless necessary",
+     {"Enable full MMU emulation and other Windows CE settings. Do not enable unless necessary",
+      "启用完整 MMU 模拟和其他 Windows CE 设置。除非必要否则不要启用"}},
+    {"Run the emulated CPU and GPU on different threads",
+     {"Run the emulated CPU and GPU on different threads",
+      "在不同线程上运行模拟的 CPU 和 GPU"}},
+    {"Dump the Dreamcast serial console to stdout",
+     {"Dump the Dreamcast serial console to stdout",
+      "将 Dreamcast 串口控制台输出转储到 stdout"}},
+    {"Do not optimize integer division. Not recommended",
+     {"Do not optimize integer division. Not recommended",
+      "不优化整数除法。不推荐"}},
+    
+    // 网络设置
+    {"Broadband Adapter Emulation", {"Broadband Adapter Emulation", "宽带适配器模拟"}},
+    {"Enable GGPO Networking", {"Enable GGPO Networking", "启用 GGPO 网络"}},
+    {"Enable Naomi Networking", {"Enable Naomi Networking", "启用 Naomi 网络"}},
+    {"Network Statistics", {"Network Statistics", "网络统计"}},
+    {"Enable UPnP", {"Enable UPnP", "启用 UPnP"}},
+    {"Enable Chat", {"Enable Chat", "启用聊天"}},
+    {"Play as Player 1", {"Play as Player 1", "作为玩家 1"}},
+    {"Act as Server", {"Act as Server", "作为服务器"}},
+    {"Create a local server for Naomi network games",
+     {"Create a local server for Naomi network games",
+      "为 Naomi 网络游戏创建本地服务器"}},
+    {"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem",
+     {"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem",
+      "模拟以太网宽带适配器 (BBA) 而不是调制解调器"}},
+    
+    // 关于页面
+    {"Version: %s", {"Version: %s", "版本：%s"}},
+    {"CPU: %s", {"CPU: %s", "CPU：%s"}},
+    {"Driver Name: %s", {"Driver Name: %s", "驱动名称：%s"}},
+    {"Build Date: %s", {"Build Date: %s", "构建日期：%s"}},
+    {"Operating System: %s", {"Operating System: %s", "操作系统：%s"}},
+    
+    // 数据目录
+    {"Data Directory", {"Data Directory", "数据目录"}},
+    {"Select Directory", {"Select Directory", "选择目录"}},
+    {"Select System Directory", {"Select System Directory", "选择系统目录"}},
+    {"The directories where your games are stored",
+     {"The directories where your games are stored",
+      "存储游戏的目录"}},
+    {"The directory containing BIOS files, as well as saved VMUs and states",
+     {"The directory containing BIOS files, as well as saved VMUs and states",
+      "包含 BIOS 文件以及保存的 VMU 和存档的目录"}},
+    {"Cannot create 'data' directory", {"Cannot create 'data' directory", "无法创建 'data' 目录"}},
+    {"Rescan Content", {"Rescan Content", "重新扫描内容"}},
+    {"Log to File", {"Log to File", "记录到文件"}},
+    {"Lua Filename", {"Lua Filename", "Lua 文件名"}},
+    {"Lua Scripting", {"Lua Scripting", "Lua 脚本"}},
+    {"Logging", {"Logging", "日志"}},
+    
+    // 其他
+    {"Loading...", {"Loading...", "加载中..."}},
+    {"Unknown", {"Unknown", "未知"}},
+    {"All", {"All", "全部"}},
+    {"Device %c", {"Device %c", "设备 %c"}},
+    {"Send Logs", {"Send Logs", "发送日志"}},
+    {"Eject Disk", {"Eject Disk", "弹出光盘"}},
+    {"Insert Disk", {"Insert Disk", "插入光盘"}},
+    {"Server", {"Server", "服务器"}},
+    {"StdDev: %.1f%%", {"StdDev: %.1f%%", "标准差: %.1f%%"}},
+    {"Connect", {"Connect", "连接"}},
+    {"Connected", {"Connected", "已连接"}},
+    {"Disconnect", {"Disconnect", "断开"}},
+    {"Disconnected", {"Disconnected", "已断开"}},
+    {"Error", {"Error", "错误"}},
+    {"Keyboard", {"Keyboard", "键盘"}},
+    {"Mouse", {"Mouse", "鼠标"}},
+    {"Light Gun", {"Light Gun", "光枪"}},
+    {"Microphone", {"Microphone", "麦克风"}},
+    {"Menu", {"Menu", "菜单"}},
+    {"Fast-forward", {"Fast-forward", "快进"}},
+    {"Left Trigger", {"Left Trigger", "左扳机"}},
+    {"Right Trigger", {"Right Trigger", "右扳机"}},
+    {"Left Stick Up", {"Left Stick Up", "左摇杆上"}},
+    {"Left Stick Down", {"Left Stick Down", "左摇杆下"}},
+    {"Left Stick Left", {"Left Stick Left", "左摇杆左"}},
+    {"Left Stick Right", {"Left Stick Right", "左摇杆右"}},
+    {"Map Axis", {"Map Axis", "映射轴"}},
+    {"Map Button", {"Map Button", "映射按钮"}},
+    {"NTSC", {"NTSC", "NTSC"}},
+    {"PAL", {"PAL", "PAL"}},
+    {"Japan", {"Japan", "日本"}},
+    {"USA", {"USA", "美国"}},
+    {"Europe", {"Europe", "欧洲"}},
+    {"English", {"English", "英语"}},
+    {"German", {"German", "德语"}},
+    {"French", {"French", "法语"}},
+    {"Spanish", {"Spanish", "西班牙语"}},
+    {"Italian", {"Italian", "意大利语"}},
+    {"LOADING... ", {"LOADING... ", "加载中... "}},
+    {"Game load failed", {"Game load failed", "游戏加载失败"}},
+    {"GAMES", {"GAMES", "游戏"}},
+    {"Flycast", {"Flycast", "Flycast"}},
+    {"Enable networking for supported Naomi games",
+     {"Enable networking for supported Naomi games",
+      "为支持的 Naomi 游戏启用网络"}},
+    {"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms",
+     {"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms",
+      "启用 Dreamcast 数字声音处理器。仅推荐在快速平台上使用"}},
+    {"Load custom/high-res textures from data/textures/<game id>",
+     {"Load custom/high-res textures from data/textures/<game id>",
+      "从 data/textures/<game id> 加载自定义/高分辨率纹理"}},
+    {"Modify the game so that it displays in 16:9 anamorphic format and use horizontal screen stretching. Only some games are supported.",
+     {"Modify the game so that it displays in 16:9 anamorphic format and use horizontal screen stretching. Only some games are supported.",
+      "修改游戏使其以 16:9 变形格式显示并使用水平屏幕拉伸。仅支持部分游戏。"}},
+    {"Maximum number of threads to use for texture upscaling. Recommended: number of physical cores minus one",
+     {"Maximum number of threads to use for texture upscaling. Recommended: number of physical cores minus one",
+      "用于纹理放大的最大线程数。推荐：物理核心数减一"}},
+    {"Internal render resolution. Higher is better but more demanding",
+     {"Internal render resolution. Higher is better but more demanding",
+      "内部分辨率。越高越好但对性能要求更高"}},
+    {"Hide .bin, .dat and .lst files from the content browser",
+     {"Hide .bin, .dat and .lst files from the content browser",
+      "在内容浏览器中隐藏 .bin、.dat 和 .lst 文件"}},
+    {"Dump all textures into data/texdump/<game id>",
+     {"Dump all textures into data/texdump/<game id>",
+      "将所有纹理转储到 data/texdump/<game id>"}},
+    {"Incorrect Content Location?", {"Incorrect Content Location?", "内容位置不正确？"}},
+    {"Home Directory", {"Home Directory", "主目录"}},
+    {"MIPS", {"MIPS", "MIPS"}},
+    {"Name", {"Name", "名称"}},
+    {"Port", {"Port", "端口"}},
+    {"Sega Controller", {"Sega Controller", "世嘉控制器"}},
+    {"Purupuru", {"Purupuru", "震动包"}},
+    {"STARTING...", {"STARTING...", "启动中..."}},
+    {"LOADING... ", {"LOADING... ", "加载中... "}},
+    {"STARTING NETWORK...", {"STARTING NETWORK...", "启动网络中..."}},
+    {"Press Start to start the game now.", {"Press Start to start the game now.", "按开始键立即开始游戏。"}},
+    {"Renderer: %s", {"Renderer: %s", "渲染器：%s"}},
+    {"Output texture size: %d x %d", {"Output texture size: %d x %d", "输出纹理大小：%d x %d"}},
+    {"Waiting for %s '%s'...", {"Waiting for %s '%s'...", "等待 %s '%s'..."}},
+    {"axis", {"axis", "轴"}},
+    {"button", {"button", "按钮"}},
+    {"Reload", {"Reload", "重新加载"}},
+    {"Right DPad Up", {"Right DPad Up", "右方向键上"}},
+    {"Right DPad Down", {"Right DPad Down", "右方向键下"}},
+    {"Right DPad Left", {"Right DPad Left", "右方向键左"}},
+    {"Right DPad Right", {"Right DPad Right", "右方向键右"}},
+    {"Right DPad X", {"Right DPad X", "右方向键 X"}},
+    {"Right DPad Y", {"Right DPad Y", "右方向键 Y"}},
+    {"Right Stick X", {"Right Stick X", "右摇杆 X"}},
+    {"Right Stick Y", {"Right Stick Y", "右摇杆 Y"}},
+    {"Left Stick X", {"Left Stick X", "左摇杆 X"}},
+    {"Left Stick Y", {"Left Stick Y", "左摇杆 Y"}},
+    {"A", {"A", "A"}},
+    {"B", {"B", "B"}},
+    {"X", {"X", "X"}},
+    {"Y", {"Y", "Y"}},
+    {"Z", {"Z", "Z"}},
+    {"C", {"C", "C"}},
+    {"D", {"D", "D"}},
+    {"VGA", {"VGA", "VGA"}},
+    {"RGB Component", {"RGB Component", "RGB 分量"}},
+    {"TV Composite", {"TV Composite", "TV 复合"}},
+    {"Japanese", {"Japanese", "日语"}},
+    {"Spanish", {"Spanish", "西班牙语"}},
+    {"NTSC", {"NTSC", "NTSC"}},
+    {"PAL", {"PAL", "PAL"}},
+    {"PAL/M", {"PAL/M", "PAL/M"}},
+    {"PAL/N", {"PAL/N", "PAL/N"}},
+    {"Purupuru", {"Purupuru", "震动包"}},
+    {"Sega VMU", {"Sega VMU", "世嘉 VMU"}},
+    {"Sega Controller", {"Sega Controller", "世嘉控制器"}},
+    {"Left Stick Down", {"Left Stick Down", "左摇杆下"}},
+    {"Left Stick Left", {"Left Stick Left", "左摇杆左"}},
+    {"Left Stick Right", {"Left Stick Right", "左摇杆右"}},
+    {"Left Stick Up", {"Left Stick Up", "左摇杆上"}},
+    {"Right Stick X", {"Right Stick X", "右摇杆 X"}},
+    {"Right Stick Y", {"Right Stick Y", "右摇杆 Y"}},
+    {"Left Stick X", {"Left Stick X", "左摇杆 X"}},
+    {"Left Stick Y", {"Left Stick Y", "左摇杆 Y"}},
+    {"Right DPad Up", {"Right DPad Up", "右方向键上"}},
+    {"Right DPad Down", {"Right DPad Down", "右方向键下"}},
+    {"Right DPad Left", {"Right DPad Left", "右方向键左"}},
+    {"Right DPad Right", {"Right DPad Right", "右方向键右"}},
+    {"Right DPad X", {"Right DPad X", "右方向键 X"}},
+    {"Right DPad Y", {"Right DPad Y", "右方向键 Y"}},
+    {"Reselect", {"Reselect", "重新选择"}},
+    {"Home Directory", {"Home Directory", "主目录"}},
+    {"Auto load/save state", {"Auto load/save state", "自动加载/保存存档"}},
+    {"Dreamcast Devices", {"Dreamcast Devices", "Dreamcast 设备"}},
+    {"Physical Devices", {"Physical Devices", "物理设备"}},
+};
+
+inline bool isChineseLocale() {
+    static int cached = -1;
+    if (cached == -1) {
+        setlocale(LC_ALL, "");
+        const char* locale = setlocale(LC_ALL, nullptr);
+        if (locale == nullptr || strcmp(locale, "C") == 0 || strcmp(locale, "POSIX") == 0)
+            locale = getenv("LANG");
+        cached = (locale != nullptr && strstr(locale, "zh") != nullptr) ? 1 : 0;
+    }
+    return cached == 1;
+}
+
+inline const char* tr(const char* english) {
+    if (!isChineseLocale())
+        return english;
+    auto it = translations.find(english);
+    if (it != translations.end())
+        return it->second.zh;
+    return english;
+}
